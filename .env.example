# Event Ticket Docker Compose Environment Variables
# Copy this file to .env and update values

# Database password (used by both postgres and api services)
DB_PASSWORD=event_ticket_secret

# API server settings
DEBUG=false
WORKERS=2

# Optional: Override default ports
# API_PORT=8010
# WEB_PORT=80
